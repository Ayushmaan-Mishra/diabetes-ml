# diabetes-ml
This project is a Streamlit-based web application that uses a Support Vector Machine (SVM) model to predict whether a person is diabetic from medical input features. It demonstrates data preprocessing, model training, and real-time prediction, showcasing a complete end-to-end machine learning workflow for healthcare decision support.
